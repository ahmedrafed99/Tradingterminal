# Ship Changes
1. Run git status and git diff to review changes
2. Create a conventional commit with a concise message
3. Push to both remotes (origin and the secondary remote)
4. Update relevant docs in /docs if behavior changed
